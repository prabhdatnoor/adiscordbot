module.exports = (what) => {
    var date = new Date();
    if (what === 'stamp') {

    } else if (what === '')
        switch (what) {
            case 'stamp':
                date.get
        }

}

var date = new Date();
console.log(date.getTime());