module.exports = (what) => {
    var date = new Date();
    if (what === 'stamp') {

    } else if (what === '')
        switch (what) {
            case 'stamp':
                date.get
        }

}